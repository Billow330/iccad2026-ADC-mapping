# ── Model weights (download via HuggingFace, see README) ──
NeuroSim/model_cache/

# ── Chat / AI assistant history ──
chat_history/
.claude/

# ── LaTeX build artifacts ──
paper/*.aux
paper/*.log
paper/*.blg
paper/*.bbl
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
NeuroSim/texput.log

# ── Compiled PDF and submission packages (regeneratable) ──
paper/main.pdf
paper/main.pdf.bak_current
paper/*.tar.gz
paper/*.zip

# ── LaTeX backup files ──
paper/*.bak_*
paper/*.backup_*
paper/*.pre_*
paper/fantaog.code-workspace*

# ── Intermediate PDF versions ──
paper/iccad_v2_*.pdf

# ── Preview PNGs (regeneratable from PDFs) ──
paper/*_preview.png

# ── Python cache ──
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# ── Vendored libraries (install via pip) ──
NeuroSim/pytorch-quantization/
NeuroSim/Documents/

# ── Compiled C/C++ objects and binaries ──
*.o
*.so
*.a
NeuroSim/NeuroSIM/main

# ── OS files ──
.DS_Store
Thumbs.db

# ── Temp / swap files ──
*.tmp
*.swp
*~

# ── pip stray files ──
=*

# ── PyTorch scale checkpoints in smoke/legacy results ──
NeuroSim/results/smoke_*/smooth_scales_*.pt
NeuroSim/results/smoke_bal/smooth_scales_*.pt
NeuroSim/results/smoke_p99/smooth_scales_*.pt
NeuroSim/results/smoke_sat_lambda/smooth_scales_*.pt
NeuroSim/results/smoke_v2/smooth_scales_*.pt
NeuroSim/results/gpt2/smooth_scales_*.pt
NeuroSim/results/gpt2_p99/smooth_scales_*.pt
NeuroSim/results/opt125m/smooth_scales_*.pt
